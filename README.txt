1. Edit `index.js` and set `APP_ID` and `APP_SECRET`
2. `node index.js`
3. Open the web link, give permissions and copy the `?code` query parameter value after the redirect to `localhost`
4. Paste the `code` into the console
5. Profit?!